# json.array! @posts, :name, :details, :quickword, :catchcopy, :start_time, :finish_time, :member, :place, :category, :image, :release, :price, :like_users,
# :join_users, :reviews

# json.posts @posts do |post|
#     json.extract! post, :id, :name, :details, :quickword, :catchcopy, :start_time, :finish_time, :member, :place, :category, :image, :release, :price, :like_users, :join_users, :reviews
#   end
