import { shallowMount } from '@vue/test-utils'
import PostCreateComponent from '~/components/layouts/PostCreateComponent.vue'

describe('FormSubmitter', () => {
  it('フォームを送信する', () => {
    const wrapper = shallowMount(PostCreateComponent)
    // 開発中
  })
})
